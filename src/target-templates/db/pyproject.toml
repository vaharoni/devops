[project]
name = "db"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.12"
dependencies = ["prisma (>=0.15.0,<0.16.0)"]

[tool.poetry.scripts]
generate = "db.scripts:generate"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
