{
  "name": "db",
  "main": "db-client.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "prisma generate --generator js-client",
    "migrate-deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "prisma": "^6.5.0"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}